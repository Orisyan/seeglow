# 更新日志

## v1.1.0 (2026-08-27)

### 新增：📚 期末冲刺（教师 PPT / 往年卷融合）

总结完视频后，可上传老师发的资料，与视频内容融合出备考材料：

- **备考融合包**：考点三方对照表（视频讲到✓ / 教师PPT强调⚠️ / 往年卷考过❌）+ 「视频没讲但老师强调」的盲区自学清单 + 划重点清单；每条内容标注来源（[mm:ss] 或 PPT 页码），不编造出处
- **押题卷**：AI 研究往年卷的题型/分值/难度分布后出新模拟卷，每题带折叠答案与解析、附「题目→考点→视频位置」复习指向
- **问答升级**：教师资料并入 AI 问答上下文——刷卷遇到不会的题，直接问"这个考点视频哪里讲了"，回答带 [mm:ss] 定位

支持 pptx / docx / pdf / txt / 图片（≤30MB）；扫描版 PDF 与图片由多模态模型逐字转写。
每份笔记最多关联 5 个文件；解析结果存 `.docs.json` 不污染字幕时间轴。

实现：新增 `seeglow/studydoc.py`（OOXML 零依赖解析 + PyMuPDF 文字/扫描双路）、
`/api/upload_doc` `/api/study_pack` 接口、前端"期末冲刺"入口。均受付费门禁保护。

---

## v1.0.2 (2026-08-27)

### 新增：永久激活码档位

- 作者侧生成器新增 `--plan lifetime`：签发**永久码**（内部到期日记为 9999-12-31，校验逻辑零特判），给愿意一次性买断的用户使用
- 前端激活成功提示与状态条对永久票显示「永久有效 / 永久」，不再出现刺眼的 9999 年日期
- 永久码同样不绑定设备、多机通用，可随时通过撤销名单作废
- 线上已部署并验证：永久码激活 → status 返回 expiry=9999-12-31 ✓

---

## v1.0.1 (2026-08-27)

### 调整：激活码不绑定设备

- 激活码改为**一码多设备通用**：手机、电脑等任意设备都能用同一张码激活，无需机器码；请把码保存在备忘录，新设备打开网站输入即可
- 订阅票据（订单号验证所得）同样不再绑定设备，凭票据 JSON 本身即可在任意设备使用
- 防滥用改由服务端承担：激活限频（10 次/分钟）+ **撤销名单**（`revoke_code` 可作废泄露/退款的码），已激活的票据到自身到期日为止
- 设备指纹仅作兼容保留，前端激活弹窗已隐藏机器码行

---

## v1.0.0 (2026-08-27)

### 新增：网站付费模式（官方 API 代答 + 订阅解锁）

在线版从「访客自带 Key」升级为「服务器内置官方 API，赞助后使用」：

- **用户无需任何 API Key**：打开即用界面，激活后由服务器官方 API 完成总结（配置走 Modal Secret，对访客完全隐身）
- **订阅解锁**：爱发电赞助 ¥29.9/月 → 激活页粘贴订单号在线秒开；按购买月数签发 35 天/月的浏览器票据
- **激活码双通道**：作者可离线签发激活码（不依赖爱发电），输入机器码 + 激活码即可激活
- **无账号体系**：授权票据（HMAC 签名的 JSON）存于各设备浏览器 localStorage，免注册、跨设备无需同步；每份授权限绑 3 台设备，换机需联系作者
- **门禁范围**：所有总结/AI 功能（含视频总结、批量、本地文件、AI 问答、改写、闪卡）未激活一律 402；涉密接口（读笔记、导出）同时沿用设备隔离
- **防伪造**：票据与激活码均由服务端 SECRET HMAC 签名；AUTH_SECRET 仅存在于 Modal Secret，永不出现在代码仓库

### 开发者

- 新增 `seeglow/pro_web.py`：爱发电 OpenAPI 订单验证、票据/激活码签发校验
- 新增 `deploy_modal.py`：Modal 一键部署描述（付费模式开启）
- 本地测试：`test_v100_paidgate.py`（TestClient 全链路）、`python test_v100_live.py`（线上冒烟）

---

## v0.9.0 (2026-08-27)

### 新增：Web 公共部署模式（手机/平板随时用）

拾光的重活全在服务端，浏览器只是界面——现在可以把它部署成网站，手机/平板浏览器直接访问，无需安装任何 App。新增环境变量 `SEELOW_PUBLIC=1` 进入公共模式：

- **BYOK 自带钥匙**：服务端配置接口锁定；每位访客在「设置」里填自己的 API 地址与 Key，仅保存在各自浏览器的 localStorage，随每次总结请求携带（白名单字段注入流水线），服务器不留存
- **结果隔离**：历史列表不再暴露；每份笔记/问答/改写/闪卡/字幕导出只允许发起任务的设备 IP 或携带任务令牌者访问
- **限额防护**：全局并发上限、每 IP 启动频率限制（默认 10 次/10 分钟）、批量上限 6 支、上传 ≤200MB，均可通过环境变量调整
- **移动端界面**：全新 640px 断点响应式布局（按钮全宽、卡片操作横滑、输入堆叠），公共站显示提示条
- **无桌面兜底**：`save_export` 在无图形环境下返回 cancelled，前端自动回退浏览器下载

部署方式见 `部署-手机访问.md`：Docker 一键（新增 Dockerfile）/ Hugging Face Spaces / VPS + Caddy HTTPS；局域网用法 `python -m seeglow --web --host 0.0.0.0` 不变。

---

## v0.8.0 (2026-08-27)

### 修复：取消任务即时生效
- 「取消任务」此前只改了个标志位却没人检查：字幕总结路径完全没有取消点（AI 总结一旦开始就无法中断）、单段音频直听全程无检查点。现已把 `stop_check` 打通到所有 LLM 调用前后
- 多段并行直听改为每 0.5 秒轮询取消状态，点击后**秒级终止**，不再等最慢的那段请求跑完
- 前端点击「取消任务」立即禁用并显示「正在取消…」

### 新增：画面理解（AI 看题目/板书）
- 无字幕视频由"纯听音频"升级为可同时**看画面**：抽取视频关键帧拼成截图墙，随音频一起交给多模态模型
- 专为速成课设计：模型会**逐字转写**屏幕上出现的题目、选项、公式、代码与板书，并标注 `[mm:ss]` 时间点——配合 ⚡速学模式即可生成带原题的自测材料
- 关键帧按"画面变化检测"挑选（换页/PPT 切换才采帧），静态课程不会产生冗余图片；每段截图墙上限 30 帧，JPEG 拼图压缩上传
- 三级自动降级：截图墙 + 音频 → 模型对图片报错则本会话内自动退回纯听 → 视频流取不到时静默纯听，总结永不因画面失败而中断
- 设置页新增开关「画面理解」，默认关闭；仅对无字幕视频（AI 直听路径）和本地音视频文件生效，需模型支持图片输入（Qwen3-Omni 支持）

---

## v0.6.0 (2026-08-23)

### 新增：速学模式
- 新总结风格「⚡ 速学模式」：★重要度标注的必会知识点 + 纯干货时间线 + 自测五题（引用格式答案，先自测后对照），3 分钟吃透一支视频
- 基础总结 prompt 支持风格覆盖基础结构（风格要求的新增章节必须输出）

### 调整：试用与定价（封装版）
- 免费试用从"共 3 次"改为**每天 3 次**（次日自动恢复）
- 下架永久档，仅保留 月度 ¥9.9 / 年度 ¥69（与行业订阅制对齐）

---

## v0.5.0 (2026-08-23)

### 新增：一键改写平台文案
- 任意笔记一键改写成：**小红书笔记**（emoji+标签）、**公众号文章**、**微博短文案**、**知乎回答**
- 保留时间点引用，直接复制发布——生成的文案本身就是内容创作者的素材

### 新增：字幕导出
- 「B站字幕」来源的笔记可导出 **SRT / VTT** 字幕文件（AI 直听来源无逐句字幕，会明确提示）

---

## v0.4.3 (2026-08-23)

### 修复

- v0.4.2 引入的前端语法错误导致**所有按钮失灵**（开始总结/设置等点击无反应）——`exportOPML` 使用了 `await` 但未声明为 `async`
- 打包脚本新增前端 JS 语法检查门禁，此类错误今后无法进入发布版

---

## v0.4.2 (2026-08-23)

### 修复：EXE 窗口内导出无反应

- 封装版窗口（WebView2）默认拦截文件下载，导致「下载 .md / 导图 .opml / 闪卡.txt」点击无反应
- 现在导出走**系统「另存为」对话框**直接保存到用户选择的位置，保存后提示完整路径；纯浏览器环境自动回退为浏览器下载
- 同时开启了 WebView 的原生下载开关作为兜底

---

## v0.4.1 (2026-08-23)

### 修复

- **下载失败**：「下载 .md」「闪卡 .txt」在部分浏览器/WebView 下点击无反应或下载空文件——原因是创建下载链接后立即释放了 URL，改为延迟释放
- **导图结构**：OPML 思维导图此前所有节点平铺无层级，现按标题层级真正嵌套（导入 XMind 后是树形导图）；标题含 `"` 时破坏 XML 的问题一并修复

---

## v0.4.0 (2026-08-23)

### 新增：时间戳一键跳转
- 总结里的 `[mm:ss]` 时间点全部变成链接，点击直接打开B站对应时间点
- 从历史记录打开的旧笔记同样支持跳转

### 新增：本地音视频文件总结
- 拖拽或选择本地 mp4/mkv/flv/mp3/wav/m4a 等文件，AI 直接聆听生成笔记
- 完全本地处理（临时文件用完即删），支持课程录像、播客、录音

### 新增：总结风格模板
- 五种风格一键切换：通用笔记 / 学习笔记（含术语表）/ 教程步骤（含操作步骤与常见坑）/ 会议纪要（含结论待办）/ 轻松杂谈（含名场面）

### 新增：收藏夹 / 合集批量总结
- 粘贴合集或收藏夹链接 → 勾选视频 → 批量总结，自动生成目录索引
- 粘贴合集内任一视频也会自动识别所属合集，一键批量

### 新增：弹幕高能时间轴
- 总结完成后展示弹幕密度热力条，一眼看出全片高能位置
- 点击色块直接跳转对应时间点；标注 Top 高能点

### 新增：导出增强
- 思维导图：一键导出 .opml（可导入 XMind / FreeMind / 幕布）
- Anki 闪卡：AI 从笔记生成问答卡，导出 .txt 直接导入 Anki 复习

### 新增：追更检查
- 添加关注的 UP 主，一键检查近 14 天新视频并批量总结
- 需要配置 SESSDATA（B站登录 Cookie）

### 新增：浏览器插件
- Chrome / Edge 加载「浏览器插件」文件夹，B站视频页一键发送到拾光总结

### 优化
- 历史笔记打开时自动恢复视频信息（时间戳跳转 + 弹幕时间轴）
- 批量任务单支失败不中断，最后汇总失败清单

---

## v0.3.0 (2026-08-22)

### 新增：AI 视频问答

- 总结完成后可继续追问："XX概念在视频哪里提到？""我这么理解对吗？"
- 回答自动引用 `[mm:ss]` 时间点，基于该视频的真实内容作答
- 上下文随总结自动保存（`.ctx.json`），历史笔记重新打开也能继续提问

### 新增：免安装 EXE 支持

- 新增交互式命令行模式与打包脚本 `build_exe.ps1`
- 打包后配置与输出跟随 EXE 目录，拷到任何 Windows 电脑可用

---

## v0.2.0 (2026-08-22)

### 新增：B站多P视频完整适配

- **网页版**：粘贴多P视频链接后，自动解析出全部分P列表，可视化选择「总结选中的P」或「总结全部P」
- **全部P模式**：逐P获取字幕/AI直听，合并为一份带 `P1 / P2 …` 章节标题的完整笔记
- **命令行**：新增 `--p N`（指定分P）与 `--all-pages`（全部P）参数
- 多P笔记文件名自动带 `_全P` 或 `_P{n}` 后缀，方便归档

### 优化

- 单P进度条按分P数加权显示，多P任务不再"卡在半路"
- 无字幕视频的音频流改选最低码率（下载更快，语音内容无损）

---

## v0.1.0 (2026-08-21)

首个公开版本。

- B站链接解析（链接/BV号/av号/b23.tv短链，支持 ?p= 分P）
- 三级内容获取：B站字幕 → 多模态大模型直听音频 → 明确报错指引
- 音频按静音点切段、并行直听后汇总；MP3 压缩上传提速
- OpenAI 兼容 API，服务商预设一键填好（硅基流动/OpenAI）
- 本地 Web 界面 + 命令行双入口
- 结构化输出：一句话总结 / 核心要点 / 内容时间线 / 金句摘录 / 适合谁看

# 拾光 SeeGlow · 手机/平板使用与公共部署指南

拾光的重活（B站接口、音频下载、AI 调用）全部在服务端完成，浏览器只是界面。
所以手机/平板**不需要装任何 App**——有两个入口：

---

## 方式一：局域网直连（最简单，30 秒）

适合：自己家里/宿舍有电脑常开，手机平板在同一 Wi-Fi 下使用。

1. 电脑上启动（默认 8765 端口）：

   ```
   python -m seeglow --web --host 0.0.0.0
   ```

2. 查看电脑的内网 IP（Windows：`ipconfig` 看 IPv4 地址，如 `192.168.1.20`）
3. 手机/平板浏览器打开 `http://192.168.1.20:8765`
4. 首次 Windows 会弹防火墙提示，勾选「专用网络」允许

> 注意：这是明文 HTTP 且无鉴权，仅限可信局域网；API Key 与笔记都在电脑上，不会外传。

## 方式二：部署成公网网站（推荐，手机随时用）

设置环境变量 `SEELOW_PUBLIC=1` 进入**公共模式**，与私有模式的区别：

| | 私有模式（默认） | 公共模式 `SEELOW_PUBLIC=1` |
|---|---|---|
| API 配置 | 存服务器 config.json | **锁定**，用户自备 Key（BYOK），只存各自浏览器 localStorage |
| SESSDATA | 本机 | 公共站不支持 |
| 历史记录 | 全部可见 | 不暴露列表；笔记只有创建任务的设备/IP 能读 |
| 并发/限流 | 无限制 | 同时任务数、每 IP 启动频率受限；批量上限 6 支；上传 ≤200MB |
| 监听地址 | 127.0.0.1 | 自动改 0.0.0.0 |

### Docker 一键部署（任何 VPS）

```bash
cd 开源发布版
docker build -t seeglow .
docker run -d -p 8765:8765 \
  -e SEELOW_PUBLIC=1 \
  -v seeglow_data:/data \
  --name seeglow seeglow
```

然后手机浏览器访问 `http://你的服务器IP:8765`。生产建议套一层 HTTPS：

```bash
# Caddy 示例（自动申请证书），Caddyfile 两行：
#   your-domain.com {
#       reverse_proxy 127.0.0.1:8765
#   }
```

### 可调限额（环境变量）

| 变量 | 默认 | 说明 |
|---|---|---|
| `SEELOW_MAX_CONCURRENT` | 2 | 全局同时进行的任务数 |
| `SEELOW_RATE_PER_IP` | 10 | 每 IP 每 10 分钟可启动的任务数 |
| `SEELOW_OUTPUT_DIR` | ./拾光 | 笔记输出目录（容器内挂 /data） |

### 其他平台

- **Modal（已部署 ✓，免费 $30/月额度，无需绑卡）**：
  当前线上地址：`https://orisyan0612--seeglow-web-api.modal.run`
  - 部署/更新：在本目录执行 `python -m modal deploy deploy_modal.py`
  - 笔记保存在 `seeglow-data` Volume，容器休眠不丢；空闲 5 分钟自动休眠，下次访问冷启动约 10~30 秒
  - 任务状态在单容器内存里（`max_containers=1`），并发能力靠 `@modal.concurrent(max_inputs=32)`
- **Hugging Face Spaces**：新建 Space → 选 Docker → 把本目录推上去即可（免费 CPU 够用，重活是调 LLM API）
- **Railway / Fly.io**：用本目录的 Dockerfile

### 用户侧怎么用（手机）

打开网站 → 右上角「设置」填一次 API Key（存在手机本地）→ 粘贴 B 站链接开始总结。
换手机不影响：Key 在各设备各自的浏览器里。

---

## 安全提示

- 公共模式务必配 HTTPS 反代（防 API Key 明文传输被嗅探）
- B 站 SESSDATA 属于登录态凭证，公共模式下已禁用，不要手工加回
- 所有导出类接口（问答/改写/闪卡/字幕/读取笔记）都要求来自创建任务的设备或携带任务令牌，陌生访客无法翻阅别人的总结

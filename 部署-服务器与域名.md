# 拾光 SeeGlow · 服务器与域名购买/部署渠道指南

> 结论先行：**买国内大厂轻量服务器（阿里云/腾讯云新人价约 ¥50~100/年）+ 阿里云万网域名（首年 ¥10~30），
> 用开源版自带的 `deploy.sh` 一键部署**。国内访问速度最快；不想备案就用香港/海外轻量节点。

## 一、服务器（二选一）

### 方案 A：国内轻量云（推荐，速度最快）

| 平台 | 推荐机型 | 参考价 | 备注 |
|---|---|---|---|
| 阿里云 轻量应用服务器 | 2核2G，峰值 3M 带宽 | 新人 ¥38~99/年 | 需要「个人实名认证」；**域名绑国内服务器必须 ICP 备案**（免费，线上办理约 1~2 周） |
| 腾讯云 轻量应用服务器 | 2核2G，4M 带宽 | 新人 ¥38~112/年 | 同上；备案流程略简化 |

- 购买入口：阿里云 → 搜「轻量应用服务器」；腾讯云 → 同名
- 选 **Ubuntu 22.04** 镜像；在防火墙放行 80/443 端口
- 备案：在云厂商控制台提交（免费），期间可先用 IP 访问调试

### 方案 B：香港/海外轻量节点（免备案，当天可用）

| 平台 | 说明 |
|---|---|
| 阿里云/腾讯云 **香港** 轻量 | 约 ¥24~34/月，免备案，国内延迟 30~80ms，速度可接受 |
| CloudCone / RackNerd 等 | 年付 $10~20 起，便宜但国内速度不稳定 |

> 用户主要在国内 → 优先方案 A + 备案；不想等备案 → 方案 B 香港节点。

## 二、域名

| 注册商 | 参考价 | 备注 |
|---|---|---|
| **阿里云万网** | `.com` 首年 ¥55~79，`.cn`/`.top` 首年 ¥9~29 | 与服务器同厂，备案/解析一站式 |
| 腾讯云 DNSPod | `.cn` 首年 ¥10~30 | 同上 |
| Cloudflare Registrar | 成本价 `.com` ≈ $10/年 | 续费不涨价；但需外币卡，且配合海外节点 |
| Namecheap | `.com` 首年 $6~9 | 海外节点用 |

- 域名解析：控制台加一条 A 记录指向服务器公网 IP 即可
- 备案要求域名注册商在国内（万网/DNSPod），且持有 ≥3 个月

## 三、部署（自动 HTTPS）

域名解析好后，在服务器上两条命令：

```bash
# 上传 开源发布版/ 整个文件夹到服务器（scp/sftp/网盘均可）后：
cd 开源发布版
sudo bash deploy.sh 你的域名.com
```

脚本自动完成：装 Docker + Caddy → 构建镜像 → 启动容器（持久化 /data）→ 配置 Caddy 自动申请 HTTPS 证书。
完成后访问 `https://你的域名` 即为官网首页，`/app` 为应用。

**启动前注入密钥**（编辑 deploy.sh 的 docker run 段或 `docker exec` 后补）：

```
SEELOW_AUTH_SECRET=<64位随机串>     # 与激活码工具 secret.txt 一致
SEELOW_API_BASE=https://api.siliconflow.cn/v1
SEELOW_API_KEY=<你的硅基流动Key>
AFDIAN_USER_ID=<爱发电开发者ID>
AFDIAN_TOKEN=<爱发电开发者Token>
```

## 四、与现有 Modal 免费站的关系

- Modal 站（orisyan0612--seeglow-web-api.modal.run）继续免费可用，作为演示/备用
- 自有域名站 = 正式站：速度、自主权、品牌都更好；两者代码同源，更新流程一致

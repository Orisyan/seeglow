# 拾光 SeeGlow · 服务器与域名购买/部署渠道指南

> 结论先行：**香港轻量服务器（免备案）+ 任意注册商域名 + `deploy.sh` 一键部署**。
> 香港节点国内访问延迟 10~50ms，当天购买当天可用，无需 ICP 备案。

## 一、香港服务器推荐（2026 实测价格）

| 平台 | 套餐 | 参考价 | 说明 |
|---|---|---|---|
| **腾讯云 轻量应用服务器** ⭐推荐 | 2核2G / 30M大带宽，直连线路 | **活动价 ¥99/年**（约 ¥8/月）；锐驰型 200Mbps 约 ¥40/月 | 大陆访问延迟低至 10~20ms，口碑最好；[购买入口](https://cloud.tencent.com/product/lighthouse)（地域选**中国香港**） |
| 阿里云 轻量应用服务器 | 2核0.5~2G / 200M峰值 | 低至 **¥25/月**，活动约 ¥99/年 | BGP 优化线路；[购买入口](https://www.aliyun.com/product/swas)（地域选中国香港） |
| 腾讯云特惠活动页 | 2核2G5M × 3年 | ¥109/3年 | 见[活动页](https://cloud.tencent.com/act/pro/lhsale) |

选购要点：
- 地域选 **中国香港**；镜像选 **Ubuntu 22.04**
- 防火墙/防火墙规则放行 **80、443、22** 端口
- 新人价通常限购 1 次，新老用户均可参与的活动约 ¥99/年

## 二、域名（免备案，任意注册商）

| 注册商 | 参考价 | 备注 |
|---|---|---|
| 腾讯云 DNSPod | `.xyz`/`.top` 首年 ¥6~10，`.com` ¥55+ | 与服务器同厂，解析控制台一站式 |
| 阿里云万网 | `.top` 首年 ¥9，`.com` ¥55+ | 同上 |
| Cloudflare Registrar | `.com` 成本价 ≈ $10/年 | 续费不涨价，需外币卡 |

> 香港服务器**不需要 ICP 备案**，域名买完解析即可用。

## 三、部署步骤（10 分钟）

1. **购买服务器**后记下公网 IP；域名控制台添加 **A 记录** → 指向该 IP
2. **上传代码**：把本地 `开源发布版/` 整个文件夹上传到服务器（含 `.env.deploy`，已配好密钥）：
   ```
   scp -r "开源发布版" root@服务器IP:/root/seeglow
   ```
3. **一键部署**：
   ```bash
   ssh root@服务器IP
   cd /root/seeglow
   sudo bash deploy.sh 你的域名.com
   ```
   脚本自动：装 Docker + Caddy → 构建镜像 → 启动（密钥从 `.env.deploy` 注入）→ HTTPS 证书
4. 访问 `https://你的域名` 即为官网，`/app` 为应用。完成。

## 四、安全与运维

- `.env.deploy` 含 API Key 与激活密钥：**只存在服务器上**，不要外传；泄露 = 换 `SEELOW_AUTH_SECRET` 重跑
- 用户数据（账号/笔记）持久化在服务器 `/data/seeglow`，更新代码重跑脚本不丢
- 常用命令：`docker logs -f seeglow`（日志）、`docker restart seeglow`（重启）
- HTTPS 由 Caddy 自动申请续期，无需手动管证书

## 五、与现有 Modal 免费站的关系

- Modal 站继续作为演示/备用入口
- 自有域名站 = 正式站：国内速度更快、品牌独立、数据自主
- 激活码两站通用（`SEELOW_AUTH_SECRET` 一致）；用户数据各自独立
